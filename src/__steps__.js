/**
 * CONTEXT API
 * 1. Create context (export)
 * 2. create provider
 * 3. to access the value: useContext hook
*/

/** 
 * Share the context api to other components
 * 1. main.jsx set AuthProvider. (context name)
 * 2. inside provider access children props and then use it
 */

/**
 * integrate the password authentication
 * 1. break down then create user function
 * 2. auth provider will have auth. and register component will have email and password 
 */
